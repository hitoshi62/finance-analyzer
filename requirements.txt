streamlit==1.62.0
# PyArrow 14 is built against the NumPy 1.x ABI (needed on macOS 11 Intel).
numpy==1.26.4
pandas==2.3.3
requests==2.34.2
pytest==8.4.2
# Newer PyArrow releases no longer publish wheels for macOS 11 Intel.
pyarrow==14.0.2
